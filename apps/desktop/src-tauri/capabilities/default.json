{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the bundled src-control main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
